#ifndef _GENERAL_UTILS_H
#define _GENERAL_UTILS_H

#define IS_BETWEEN(eval, num1, num2)                                           \
    ((eval) > (num1) && (eval) <= (num2)) // num1 < eval <= num2

#endif // _GENERAL_UTILS_H
