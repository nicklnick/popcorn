/*#include "session.h"
#include <stdio.h>

int main(int argc, char const *argv[]) {
  session_ptr session = new_user_session(10);
  if (session_authenticate(session, "admin"))
    printf("Succesfully authenticated!\n");
  else
    printf("Authetincation error\n");

  return 0;
}*/
